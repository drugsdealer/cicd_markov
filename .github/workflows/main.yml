name: Stage Store CI/CD Full

on:
  push:
    branches: [ main, 'feature/*', 'hotfix/*' ]
  pull_request:
    branches: [ main ]

permissions:
  contents: write
  pull-requests: write

jobs:
  # 1) Тесты
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install deps (optional)
        run: |
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          else
            echo "requirements.txt not found, skipping"
          fi

      - name: Dummy tests
        run: echo "Tests passed"

      - name: Add PR label test-passed
        if: ${{ github.event_name == 'pull_request' }}
        uses: actions-ecosystem/action-add-labels@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          labels: test-passed

  # 2) Security
  security:
    name: Security Check
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install safety & flake8
        run: |
          python -m pip install --upgrade pip
          pip install safety flake8 || true

      - name: Safety scan
        run: safety check || true

      - name: Lint (flake8)
        run: flake8 . || true

      - name: Add PR label sec-passed
        if: ${{ github.event_name == 'pull_request' }}
        uses: actions-ecosystem/action-add-labels@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          labels: sec-passed

  # 3) Авто-версионирование
  version:
    name: Update Version
    runs-on: ubuntu-latest
    needs: security
    outputs:
      new_version: ${{ steps.bump.outputs.new_version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Read current version
        id: readver
        run: |
          if [ -f version ]; then CURR=$(cat version); else CURR="0.1.0"; echo "$CURR" > version; fi
          echo "current_version=$CURR" >> $GITHUB_OUTPUT

      - name: Bump version (minor for feature/* else patch)
        id: bump
        run: |
          OLD=${{ steps.readver.outputs.current_version }}
          IFS='.' read -r MAJ MIN PATCH <<< "$OLD"
          if [[ "${GITHUB_REF}" == refs/heads/feature/* ]]; then
            MIN=$((MIN + 1))
          else
            PATCH=$((PATCH + 1))
          fi
          NEW="$MAJ.$MIN.$PATCH"
          echo "$NEW" > version
          echo "new_version=$NEW" >> $GITHUB_OUTPUT
          echo "Updated version: $OLD -> $NEW"

      - name: Commit version file
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add version
          git commit -m "version: ${{ steps.bump.outputs.new_version }} <- ${{ steps.readver.outputs.current_version }}" || true
          git push || true

      - name: Add PR label version-updated
        if: ${{ github.event_name == 'pull_request' }}
        uses: actions-ecosystem/action-add-labels@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          labels: version-updated

  # 4) Формирование CHANGELOG + артефакт
  changelog:
    name: Generate CHANGELOG
    runs-on: ubuntu-latest
    needs: version
    outputs:
      tag: ${{ steps.buildlog.outputs.tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Build changelog entry
        id: buildlog
        run: |
          V="v${{ needs.version.outputs.new_version }}"
          DATE="$(date +'%Y-%m-%d %H:%M')"
          BRANCH="${GITHUB_REF_NAME}"
          MSG="$(git log -1 --pretty=%s)"
          SHA="$(git rev-parse --short HEAD)"
          {
            echo "## ${V} — ${DATE}"
            echo "- Branch: ${BRANCH}"
            echo "- Commit: ${SHA} — ${MSG}"
            echo
          } > .changelog_entry
          if [ -f CHANGELOG.md ]; then
            cat .changelog_entry CHANGELOG.md > .tmp && mv .tmp CHANGELOG.md
          else
            mv .changelog_entry CHANGELOG.md
          fi
          echo "tag=${V}" >> $GITHUB_OUTPUT

      - name: Commit changelog
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add CHANGELOG.md
          git commit -m "changelog: ${{ steps.buildlog.outputs.tag }}" || true
          git push || true

      - name: Upload changelog artifact
        uses: actions/upload-artifact@v4
        with:
          name: changelog
          path: CHANGELOG.md

      - name: Add PR label changelog
        if: ${{ github.event_name == 'pull_request' }}
        uses: actions-ecosystem/action-add-labels@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          labels: changelog

  # 5) Публикация в DockerHub (+обновление описания/README)
  dockerhub:
    name: Publish DockerHub
    runs-on: ubuntu-latest
    needs: [version, changelog]
    env:
      DH_USER: ${{ secrets.DOCKERHUB_USERNAME }}
      VERSION_TAG: v${{ needs.version.outputs.new_version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Normalize repo name to lowercase
        id: repo
        run: |
          LOW=$(echo "${{ github.event.repository.name }}" | tr '[:upper:]' '[:lower:]')
          echo "repo=${LOW}" >> $GITHUB_OUTPUT

      - name: Ensure README.md exists
        run: |
          if [ ! -f README.md ]; then
            echo "# ${{ github.event.repository.name }}" > README.md
            echo "" >> README.md
            echo "Automated build via GitHub Actions." >> README.md
          fi

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DH_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.DH_USER }}/${{ steps.repo.outputs.repo }}:${{ env.VERSION_TAG }}
            ${{ env.DH_USER }}/${{ steps.repo.outputs.repo }}:latest

      # попытка 1: peter-evans
      - name: Update DockerHub description (peter-evans)
        id: desc1
        uses: peter-evans/dockerhub-description@v3
        continue-on-error: true
        with:
          username: ${{ env.DH_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: ${{ env.DH_USER }}/${{ steps.repo.outputs.repo }}
          short-description: ${{ github.event.repository.description }}
          readme-filepath: README.md

      # fallback: crazy-max (если у peter-evans 403)
      - name: Update DockerHub description (fallback)
        if: ${{ steps.desc1.outcome == 'failure' }}
        uses: crazy-max/ghaction-dockerhub-description@v3
        with:
          username: ${{ env.DH_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: ${{ env.DH_USER }}/${{ steps.repo.outputs.repo }}
          readme-filepath: README.md

      - name: Add PR label dockerhub
        if: ${{ github.event_name == 'pull_request' }}
        uses: actions-ecosystem/action-add-labels@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          labels: dockerhub

  # 6) Telegram: сообщение + прикреплённый CHANGELOG.md
  notify:
    name: Notify Telegram
    runs-on: ubuntu-latest
    needs: dockerhub
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && secrets.TELEGRAM_TOKEN && secrets.TELEGRAM_TO }}
    steps:
      - name: Download changelog artifact
        uses: actions/download-artifact@v4
        with:
          name: changelog
          path: artifacts

      - name: Send Telegram
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            ✅ Выпущена новая версия: v${{ needs.version.outputs.new_version }}
            📦 Репозиторий: ${{ github.repository }}
            🔀 Ветка: ${{ github.ref_name }}
            🐳 DockerHub: https://hub.docker.com/r/${{ secrets.DOCKERHUB_USERNAME }}/${{ github.event.repository.name }}/tags
            ▶️ Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          document: artifacts/CHANGELOG.md

  # 7) GitHub Release (+attach CHANGELOG.md)
  release:
    name: GitHub Release
    runs-on: ubuntu-latest
    needs: [dockerhub, notify]
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Download changelog artifact
        uses: actions/download-artifact@v4
        with:
          name: changelog
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ needs.version.outputs.new_version }}
          name: Release v${{ needs.version.outputs.new_version }}
          body_path: artifacts/CHANGELOG.md
          files: |
            artifacts/CHANGELOG.md
